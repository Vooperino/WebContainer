[supervisord]
nodaemon=true
pidfile=/var/run/supervisord/supervisord.pid
logfile=/dev/stdout
logfile_maxbytes=0
loglevel=info

[program:supercronic]
command=supercronic -overlapping REPLACE_ME
autostart=true
autorestart=true
redirect_stderr=true